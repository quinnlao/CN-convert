请开发一个纯前端的「中文繁简转换器」Web App，并配置为可直接部署到 GitHub Pages。

========================================
一、项目定位
========================================

这是一个轻量、简洁、有设计感的本地文件繁简转换工具。

核心功能：

用户选择或拖入一个 TXT / EPUB 文件
→ 选择转换方向
→ 选择输出格式
→ 浏览器本地完成繁简转换
→ 下载转换后的文件

所有文件必须只在用户浏览器本地处理。

严禁将用户文件上传到服务器。

不需要：
- 后端
- 数据库
- 登录
- 用户系统
- API
- 云端存储
- 文件历史记录
- 服务器端文件处理

项目最终部署在 GitHub Pages，因此必须是纯静态 Web App。


========================================
二、核心原则：只转换文本，不改变原文件格式
========================================

这是本项目最重要的要求。

请将以下原则作为整个项目的核心设计原则：

「这是一个文本原位转换器，不是文档重新生成器。」

无论处理 TXT 还是 EPUB：

只允许修改需要进行繁简转换的中文文本内容。

除此之外，原始文件中的其他内容必须尽可能保持完全不变。

这里的「繁简转换」严格限定为繁体字形与简体字形之间的转换。

这是一个字形转换器，不是地区用语本地化工具，也不是表达改写工具。

转换过程中严禁：
- 将台湾、香港或大陆用语替换成其他地区的同义表达
- 替换同义词或近义词
- 改写措辞、语序、语法或句式
- 根据上下文润色、补写、删减或重述原文
- 改变作者原本的语言风格和表达习惯

例如：
- 「軟體」只能转换为「软体」，不能改写为「软件」
- 「滑鼠」转换为简体时仍应是「滑鼠」，不能改写为「鼠标」
- 「網路影片」只能转换为「网路影片」，不能改写为「网络视频」
- 「建案宣傳手冊」只能转换为「建案宣传手册」，不能改写为「楼盘宣传手册」

允许使用上下文判断同一个繁体字所对应的正确简体字形，例如：
- 「乾脆」→「干脆」
- 「帶著」→「带着」
- 「麵糰」→「面团」
- 「頭髮」→「头发」

特别禁止因为转换过程而改变原文件的：
- 排版
- 换行
- 空行
- 缩进
- 空格
- Tab
- 文档结构
- 文件结构
- 样式
- 图片
- 字体
- 元数据
- 文件路径
- 文件名称

不要采用：

原文件
→ 提取全部文字
→ 转换
→ 重新生成一个新文件

这种方式。

应该采用：

原文件
→ 读取原始结构
→ 在原结构中原位修改文本
→ 其他内容原样保留
→ 输出文件


========================================
三、支持的文件格式
========================================

第一版只支持：

输入：
- TXT
- EPUB

输出：
- TXT
- EPUB

不要在第一版加入 PDF、DOCX、MOBI、AZW3 等其他格式。

输出格式可以根据输入文件自动限制。

例如：

TXT → 只允许输出 TXT
EPUB → 只允许输出 EPUB


========================================
四、繁简转换
========================================

支持两个转换方向：

1. 简体中文 → 繁體中文
2. 繁體中文 → 简体中文

必须使用成熟的 OpenCC 实现。

不要自行编写简单的「一个繁体字对应一个简体字」映射表。

需要使用 OpenCC 的上下文字形消歧能力，正确处理「乾脆」「帶著」「麵糰」「頭髮」等不能安全依靠单字映射完成的繁简转换。

OpenCC 配置不得启用地区词汇本地化。第一版固定使用：

- 繁体中文 → 简体中文：`tw → cn`
- 简体中文 → 繁体中文：`cn → tw`

禁止使用会改写地区用语的 `twp` 等词汇本地化配置。

验收时必须确认转换只改变繁简字形。例如：

- 「軟體與滑鼠」→「软体与滑鼠」
- 「文字檔案」→「文字档案」
- 「聯絡」→「联络」
- 「訊息」→「讯息」
- 「影像」→「影像」
- 「視窗」→「视窗」

不得将上述内容改写为「软件」「鼠标」「文字文件」「联系」「消息」「图像」或「窗口」。

转换逻辑必须完全在浏览器本地执行。


========================================
五、TXT 处理要求
========================================

TXT 没有 EPUB 那样的视觉排版，但必须严格保护原始文本格式。

TXT 的处理原则：

「只修改需要转换的中文内容，其他字符保持原样。」

必须保留：

- 所有换行符
- 所有空行
- 所有空格
- 所有 Tab
- 所有缩进
- 原始段落结构
- 标点符号
- 数字
- 英文字母
- 特殊字符

禁止：

- 自动 trim
- 自动格式化
- 自动换行
- 自动合并空行
- 自动删除空格
- 自动添加空格
- 自动修改缩进
- 自动修改段落
- 自动修改标点
- 自动改变非中文字符

例如原文：

第一章　這是一個故事。

這一天，天氣很好。
　　他走在街上。

转换后应该仅发生：

第一章　这是一个故事。

这一天，天气很好。
　　他走在街上。

原本的空行、全角空格、缩进、换行必须保持。

不要因为读取和重新生成字符串而改变这些内容。


========================================
六、EPUB 处理要求
========================================

EPUB 是本项目最需要注意的部分。

EPUB 本质上是一个 ZIP 容器。

必须直接修改原 EPUB 内的正文文件，而不是重新生成一本 EPUB。

处理流程：

EPUB
→ 解压 ZIP
→ 读取 EPUB 结构
→ 找到 container.xml
→ 找到 OPF
→ 根据 OPF manifest 找到正文 XHTML/HTML
→ 遍历正文中的文本节点
→ 只转换文本节点中的中文
→ 将修改后的文本原位写回
→ 其他文件完全保留
→ 重新打包 EPUB


========================================
七、EPUB 必须保持原排版
========================================

这是硬性要求。

繁简转换绝对不能影响 EPUB 原本的排版和视觉表现。

转换前后应该尽可能保证在电子书阅读器中：

- 图片位置不变
- 图片尺寸不变
- 图片资源不变
- 字体不变
- 字号不变
- CSS 不变
- 行距不变
- 段距不变
- 页边距不变
- 对齐方式不变
- 标题样式不变
- 段落结构不变
- 章节结构不变
- 目录不变
- 页面顺序不变
- 阅读顺序不变


========================================
八、EPUB 只修改正文 Text Node
========================================

不要对整个 XHTML/HTML 进行字符串级重写。

尽可能采用 DOM Text Node 层面的处理：

例如原始：

<p class="text"><span>這是一段</span><em>繁體中文</em>。</p>

转换后必须保持 HTML 结构：

<p class="text"><span>这是一段</span><em>繁体中文</em>。</p>

只能修改：

「這是一段」
→
「这是一段」

「繁體中文」
→
「繁体中文」

禁止变成：

<p>
  <span>这是一段</span>
  <em>繁体中文</em>
  。
</p>

不要因为 DOM 序列化导致 HTML 被重新格式化、重新缩进或改变结构。

禁止：
- prettify
- format
- minify
- 自动重新缩进
- 修改标签结构
- 修改属性


========================================
九、EPUB 禁止修改的内容
========================================

以下内容原则上必须原样保留：

- HTML/XML 标签
- HTML/XML 属性
- class
- id
- style
- href
- src
- URL
- 文件路径
- CSS
- JavaScript
- 图片
- SVG
- 字体
- 音频
- 视频
- metadata
- manifest
- spine
- navigation
- NCX
- TOC
- 文件名
- 文件目录结构

不要转换属性中的文字。

例如：

<div class="chapter-title" id="chapter1">

这里的：

chapter-title
chapter1

都不能被转换。

只转换实际显示给读者的正文文本。


========================================
十、EPUB 文件结构
========================================

EPUB 中可能存在多个 XHTML/HTML 文件。

不能假设正文一定位于某一个固定目录。

必须通过：

META-INF/container.xml
→ content.opf
→ manifest / spine

正确找到需要处理的正文文档。

需要处理所有实际正文 XHTML/HTML 文件。

同时不要误处理：
- CSS
- SVG
- XML metadata
- navigation
- 图片
- 其他资源


========================================
十一、EPUB 重新打包要求
========================================

重新生成 EPUB 时必须遵守 EPUB ZIP 规范。

特别注意：

mimetype 文件必须：

1. 存在
2. 位于 ZIP 中第一个
3. 内容严格为：

application/epub+zip

4. mimetype 文件不能压缩

除正文文本发生的繁简变化之外，其他 EPUB 文件尽可能保持原始内容完全不变。


========================================
十二、编码处理
========================================

TXT 默认支持 UTF-8。

读取 TXT 时不要因为转换导致：

- 中文乱码
- BOM 被错误处理
- 换行符被改变

尽可能保持原始文本编码和换行格式。

至少需要正确处理常见的 UTF-8 TXT。

如果实现成本合理，可以增加 UTF-8 BOM 检测和保留。

不要为了所谓的编码统一而强制修改原文件内容。


========================================
十三、文件名
========================================

转换完成后自动生成新的文件名。

例如：

三体.txt
→
三体-简体.txt

三体.txt
→
三体-繁体.txt

三体.epub
→
三体-简体.epub

三体.epub
→
三体-繁体.epub

不要覆盖用户原始文件。


========================================
十四、UI 设计
========================================

UI 要求：

简洁、现代、有设计感、精致。

不要做成传统后台管理系统。

不要堆砌功能。

不要有复杂导航。

不要使用大量表格、按钮、卡片。

整体可以参考：

- Linear
- Raycast
- Apple Utilities

视觉特点：

- 大留白
- 简洁布局
- 柔和边框
- 轻微阴影
- 合理圆角
- 简洁 SVG icon
- 微妙 hover transition
- 清晰的信息层级
- 响应式
- 桌面端和移动端都能正常使用

不需要复杂 UI 组件库。

可以使用原生 CSS / CSS Variables 实现。


========================================
十五、页面结构
========================================

首页保持非常简单。

顶部：

繁简转换器

副标题：

在浏览器本地完成中文繁简转换


核心区域：

┌──────────────────────────┐
│                          │
│      将文件拖到这里       │
│                          │
│     或点击选择文件        │
│                          │
│      支持 TXT · EPUB     │
│                          │
└──────────────────────────┘


选择文件后：

显示：

- 文件图标
- 文件名
- 文件大小
- 删除/重新选择按钮


然后：

转换方向

[ 简体 → 繁体 ] [ 繁体 → 简体 ]


输出格式

根据输入文件自动确定。


最后：

[ 开始转换 ]


========================================
十六、转换状态
========================================

转换过程中显示简单的进度状态。

例如：

正在读取文件……

正在处理 EPUB……

正在处理第 12 / 23 章……

正在重新打包……

转换完成。


不要做复杂的进度系统。

如果无法准确计算百分比，可以使用阶段式状态，不要伪造一个不准确的百分比。


========================================
十七、转换完成
========================================

完成后显示：

✓ 转换完成

文件名：
三体-简体.epub

转换方向：
繁體中文 → 简体中文

如果是 EPUB，可以显示：

章节：23

如果能够方便统计，可以显示：

处理字符：184,293

转换耗时：1.8s


然后提供：

[ 下载文件 ]


========================================
十八、错误处理
========================================

需要处理常见错误：

- 文件格式不支持
- EPUB 结构损坏
- 无法读取 EPUB
- 无法找到 OPF
- 无法找到正文文件
- 文件读取失败
- 转换失败
- EPUB 打包失败

错误提示要对普通用户友好。

不要直接显示难以理解的 JavaScript stack trace。

例如：

「无法读取该 EPUB 文件，请确认文件未损坏。」

而不是：

「TypeError: Cannot read properties of undefined...」


========================================
十九、技术要求
========================================

使用：

- Vite
- TypeScript
- OpenCC 浏览器端实现
- JSZip 或成熟的浏览器端 ZIP 库

依赖尽可能少。

不要为了简单功能引入大型 UI Framework。

所有功能必须运行在浏览器端。

不能依赖 Node.js server runtime。

不能依赖 API。

不能上传文件。


========================================
二十、GitHub Pages
========================================

项目必须能够直接部署到 GitHub Pages。

要求：

1. 配置 Vite base，使项目能够正确运行在：

https://用户名.github.io/仓库名/

这样的 repository path 下。

2. 添加 GitHub Actions 自动部署 workflow。

3. 执行：

npm run build

必须成功。

4. GitHub Actions 自动完成：

安装依赖
→ build
→ 部署 dist

5. README 中说明：

- 本地安装
- npm install
- npm run dev
- npm run build
- GitHub Pages 部署方式


========================================
二十一、性能要求
========================================

尽可能避免一次性复制和转换大量字符串造成不必要的内存占用。

EPUB 需要逐文件处理。

界面在处理较大的 EPUB 时不要完全卡死。

如果实现成本合理，可以使用 Web Worker 将繁简转换或 EPUB 处理放到 Worker 中。

但不要为了性能过度复杂化第一版。

优先保证：

正确性
→ 原格式保持
→ 稳定性
→ UI
→ 性能优化


========================================
二十二、最重要的验收标准
========================================

完成后请自行测试至少以下情况：

TXT：

1. 普通繁体 TXT
2. 包含大量空行的 TXT
3. 包含缩进的 TXT
4. 包含 Tab 的 TXT
5. 包含中英文混排的 TXT
6. 包含数字、特殊符号的 TXT

确保转换后除了繁简文字发生变化：

其他字符、空格、换行、缩进、段落结构均没有被意外修改。


EPUB：

至少测试：

1. 多章节 EPUB
2. 包含 CSS 的 EPUB
3. 包含图片的 EPUB
4. 包含目录的 EPUB
5. XHTML 中存在 span / em / strong 等嵌套标签的 EPUB
6. 包含 metadata 的 EPUB

确保：

- EPUB 可以正常打开
- 章节正常
- 目录正常
- 图片正常
- CSS 正常
- 字体正常
- 排版正常
- 文本正常完成繁简转换


========================================
最终要求
========================================

请直接完成整个项目，不要只给出方案。

项目完成后：

1. 确保代码可以运行
2. 确保 npm run build 成功
3. 确保 GitHub Pages 配置完整
4. 确保 README 完整
5. 检查 TXT 和 EPUB 的原始格式保持逻辑
6. 特别检查 EPUB 不会因为 DOM 序列化或重新打包而破坏原有排版
7. 不要擅自增加与核心功能无关的功能

核心目标只有一个：

「让用户把一本繁体 EPUB/TXT 拖进去，选择简体或繁体，得到一本内容完成转换、但原有排版和格式尽可能完全保持不变的新文件。」
